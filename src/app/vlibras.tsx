"use client";

import VLibras from "vlibras-nextjs";

const VLibrasWidget = () => {
  return <VLibras forceOnload />;
};

export const Vlibraswifged2 = () => {
  return <VLibras forceOnload />;
};

export default VLibrasWidget;
